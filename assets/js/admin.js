/**
 * Theme settings screen behaviour.
 *
 * @package Moghadam
 * @since   1.2.0
 */

( function ( $ ) {
	'use strict';

	$( function () {
		$( '.moghadam-color-field' ).wpColorPicker();
	} );
}( jQuery ) );
